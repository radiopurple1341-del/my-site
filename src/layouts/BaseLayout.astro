---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const { title, description, image } = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <BaseHead title={title} description={description} image={image} />
  </head>
  <body>
    <Header />
    <main class="main-content">
      <div class="site-wrap">
        <slot />
      </div>
    </main>
    <Footer />
  </body>
</html>

<script>
  // Amazonアフィリエイトリンクを新規タブで開く
  document.querySelectorAll<HTMLAnchorElement>('a[href^="https://amzn.to/"]').forEach((a) => {
    a.target = '_blank';
    a.rel = 'noopener';
  });
</script>

<style>
.main-content {
  padding: 1.5rem 0 3rem;
  flex: 1;
}
</style>
